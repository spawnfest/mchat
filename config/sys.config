[
 %% Mchat config
 %% IMPORTANT: make user to make 
 %%            same changes to client-config.json
 {mchat, [
         {ip, "localhost"},
         {port, 8080},
         {ws_pool_size, 100}
        ]}
].

